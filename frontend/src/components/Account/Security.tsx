import { Box, Text } from '@chakra-ui/react';

const Security = () => {
  return (
    <Box>
      <Text fontSize="30px" color="#FFF">
        Password & Security
      </Text>
    </Box>
  );
};

export default Security;
