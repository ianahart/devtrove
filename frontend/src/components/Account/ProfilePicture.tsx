import { Box, Image, Icon, Text } from '@chakra-ui/react';
import { FaUser } from 'react-icons/fa';

const ProfilePicture = () => {
  return <Icon as={FaUser} fontSize="40px" color="yellow" />;
};

export default ProfilePicture;
